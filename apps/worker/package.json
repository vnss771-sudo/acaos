{
  "name": "@acaos/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "start": "npx prisma generate --schema ../../packages/db/prisma/schema.prisma && npx prisma migrate deploy --schema ../../packages/db/prisma/schema.prisma && tsx src/worker.ts"
  },
  "dependencies": {
    "bullmq": "^5.12.12",
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "tsx": "^4.16.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
